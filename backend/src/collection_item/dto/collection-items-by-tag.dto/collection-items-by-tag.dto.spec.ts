import { CollectionItemsByTagDto } from './collection-items-by-tag.dto';

describe('CollectionItemsByTagDto', () => {
  it('should be defined', () => {
    expect(new CollectionItemsByTagDto()).toBeDefined();
  });
});
