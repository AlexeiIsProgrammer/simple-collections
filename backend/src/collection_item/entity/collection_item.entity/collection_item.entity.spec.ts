import { CollectionItemEntity } from './collection_item.entity';

describe('CollectionItemEntity', () => {
  it('should be defined', () => {
    expect(new CollectionItemEntity()).toBeDefined();
  });
});
