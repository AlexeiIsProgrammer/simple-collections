import { CollectionItemCustomFieldEntity } from './collection_item_custom_field.entity';

describe('CollectionItemCustomFieldEntity', () => {
  it('should be defined', () => {
    expect(new CollectionItemCustomFieldEntity()).toBeDefined();
  });
});
