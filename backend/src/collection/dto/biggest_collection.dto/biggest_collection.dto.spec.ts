import { BiggestCollectionDto } from './biggest_collection.dto';

describe('BiggestCollectionDto', () => {
  it('should be defined', () => {
    expect(new BiggestCollectionDto()).toBeDefined();
  });
});
